/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["m.media-amazon.com", "www.retrogames.cz", "plum-xerothermic-louse-526.mypinata.cloud"], // Add other domains here if needed
  },
};

export default nextConfig;
