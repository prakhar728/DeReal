/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["m.media-amazon.com", "www.retrogames.cz"], // Add other domains here if needed
  },
};

export default nextConfig;
